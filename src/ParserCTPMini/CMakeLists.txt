
#1. 确定CMake的最低版本需求
CMAKE_MINIMUM_REQUIRED(VERSION 3.20)

#2. 确定工程名
PROJECT(ParserCTPMini LANGUAGES CXX)

SET(SRC  
	${PROJECT_SOURCE_DIR}/ParserCTPMini.cpp
	${PROJECT_SOURCE_DIR}/ParserCTPMini.h
)

SET(LIBRARY_OUTPUT_PATH ${CMAKE_BINARY_DIR}/build_${PLATFORM}/${CMAKE_BUILD_TYPE}/bin)

ADD_LIBRARY(ParserCTPMini SHARED ${SRC})
TARGET_LINK_LIBRARIES(ParserCTPMini PRIVATE WT::fmt)

IF (MSVC)
ELSE (GNUCC)
	SET(LIBS
		WT::BoostThread
		${FS_LIB}
		WT::Platform
	)
	TARGET_LINK_LIBRARIES(ParserCTPMini PUBLIC ${LIBS})
ENDIF()

IF (MSVC)
ELSE (GNUCC)
	SET_TARGET_PROPERTIES(ParserCTPMini PROPERTIES
		CXX_VISIBILITY_PRESET hidden
		C_VISIBILITY_PRESET hidden
		VISIBILITY_INLINES_HIDDEN 1
        LINK_FLAGS_RELEASE -s)
ENDIF ()

