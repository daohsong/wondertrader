
#1. 确定CMake的最低版本需求
CMAKE_MINIMUM_REQUIRED(VERSION 3.20)

#2. 确定工程名
PROJECT(TestParser LANGUAGES CXX)

SET(EXECUTABLE_OUTPUT_PATH ${CMAKE_BINARY_DIR}/build_${PLATFORM}/${CMAKE_BUILD_TYPE}/bin/TestParser)

#7. 添加源码
file(GLOB SRCS *.cpp)

SET(LIBS
	WTSTools
	WTSUtils
	)
    
IF (MSVC)
ELSE(GNUCC)
	LIST(APPEND LIBS
		WT::Platform
		${FS_LIB})
    IF(WIN32)
		LIST(APPEND LIBS
			iconv)
	ENDIF()
ENDIF()


ADD_EXECUTABLE(TestParser ${SRCS})
TARGET_LINK_LIBRARIES(TestParser PRIVATE WT::fmt)
TARGET_LINK_LIBRARIES(TestParser PRIVATE ${LIBS})

IF (MSVC)
ELSE (GNUCC)
	SET_TARGET_PROPERTIES(TestParser PROPERTIES
        LINK_FLAGS_RELEASE -s)
ENDIF ()